<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/chat_reply_quote_block"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="4dp"
    android:background="@drawable/bg_chat_reply_quote"
    android:clickable="true"
    android:focusable="true"
    android:orientation="horizontal"
    android:paddingStart="0dp"
    android:paddingTop="4dp"
    android:paddingEnd="6dp"
    android:paddingBottom="4dp"
    android:visibility="gone">

    <View
        android:id="@+id/chat_reply_quote_bar"
        android:layout_width="3dp"
        android:layout_height="match_parent"
        android:layout_marginEnd="8dp"
        android:background="@color/tg_reply_quote_accent" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/chat_reply_quote_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="1"
            android:textColor="@color/tg_reply_quote_name"
            android:textSize="13sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/chat_reply_quote_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="2"
            android:textColor="@color/tg_reply_quote_text"
            android:textSize="13sp" />
    </LinearLayout>

</LinearLayout>
