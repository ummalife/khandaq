<?xml version="1.0" encoding="utf-8"?>
<!-- KHANDAQ "Создание профиля" (Figma 2031:9523): app bar (← Назад) + centered wordmark + title +
     Имя field + Название профиля field (+ example hint) + primary "Далее". Mirrors the onboarding
     set-password style. CreateProfileActivity saves the name → SetPasswordActivity. -->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/onboarding_form_bg"
    android:fitsSystemWindows="true">

    <!-- App bar: back -->
    <LinearLayout
        android:id="@+id/onboarding_appbar"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingHorizontal="4dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <ImageView
            android:id="@+id/back_button"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:padding="12dp"
            android:src="@drawable/ic_arrow_back"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/onboarding_back" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="4dp"
            android:text="@string/onboarding_back"
            android:textSize="20sp"
            android:textColor="@color/onboarding_title" />
    </LinearLayout>

    <!-- Centered Khandaq wordmark -->
    <ImageView
        android:id="@+id/onboarding_logo"
        android:layout_width="wrap_content"
        android:layout_height="48dp"
        android:layout_marginTop="12dp"
        android:adjustViewBounds="true"
        android:src="@drawable/khandaq_wordmark"
        android:importantForAccessibility="no"
        app:layout_constraintTop_toBottomOf="@id/onboarding_appbar"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Title -->
    <TextView
        android:id="@+id/create_profile_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:layout_marginHorizontal="16dp"
        android:text="@string/create_profile_title"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="@color/onboarding_title"
        app:layout_constraintTop_toBottomOf="@id/onboarding_logo"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Form -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="16dp"
        android:fillViewport="true"
        app:layout_constraintTop_toBottomOf="@id/create_profile_title"
        app:layout_constraintBottom_toTopOf="@id/create_profile_next_button"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingHorizontal="16dp">

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@string/create_profile_name_label"
                android:textSize="16sp"
                android:textColor="@color/onboarding_title" />

            <EditText
                android:id="@+id/create_profile_name_field"
                android:layout_width="match_parent"
                android:layout_height="48dp"
                android:layout_marginTop="8dp"
                android:background="@drawable/bg_onboarding_field"
                android:paddingHorizontal="16dp"
                android:hint="@string/create_profile_name_hint"
                android:textColorHint="@color/onboarding_hint"
                android:textColor="@color/onboarding_title"
                android:textSize="14sp"
                android:imeOptions="flagNoPersonalizedLearning"
                android:inputType="textPersonName|textCapWords"
                android:maxLines="1"
                android:singleLine="true" />

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:text="@string/create_profile_label_label"
                android:textSize="16sp"
                android:textColor="@color/onboarding_title" />

            <EditText
                android:id="@+id/create_profile_label_field"
                android:layout_width="match_parent"
                android:layout_height="48dp"
                android:layout_marginTop="8dp"
                android:background="@drawable/bg_onboarding_field"
                android:paddingHorizontal="16dp"
                android:hint="@string/create_profile_label_hint"
                android:textColorHint="@color/onboarding_hint"
                android:textColor="@color/onboarding_title"
                android:textSize="14sp"
                android:imeOptions="flagNoPersonalizedLearning"
                android:inputType="text"
                android:maxLines="1"
                android:singleLine="true" />

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:text="@string/create_profile_label_example"
                android:textSize="14sp"
                android:textColor="@color/onboarding_hint" />
        </LinearLayout>
    </ScrollView>

    <!-- Bottom action -->
    <Button
        android:id="@+id/create_profile_next_button"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:layout_marginHorizontal="16dp"
        android:layout_marginBottom="24dp"
        android:background="@drawable/bg_onboarding_btn"
        android:text="@string/create_profile_next"
        android:textAllCaps="false"
        android:textSize="18sp"
        android:textColor="@android:color/white"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
