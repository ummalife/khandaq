<?xml version="1.0" encoding="utf-8"?>
<!-- KHANDAQ (Figma add-people / forward picker): clean letter-avatar (no border) + name over presence text. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/outside_friend_info_container"
    android:layout_width="fill_parent"
    android:layout_height="72dp"
    android:background="@null"
    android:orientation="vertical">

    <RelativeLayout
        android:id="@+id/friend_line_container"
        android:layout_width="fill_parent"
        android:layout_height="71dp"
        android:background="@null"
        android:paddingLeft="16dp"
        android:paddingRight="16dp">

        <de.hdodenhof.circleimageview.CircleImageView
            android:id="@+id/f_avatar_icon"
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:layout_centerVertical="true"
            android:layout_marginRight="14dp"
            app:civ_border_width="0dp" />

        <!-- legacy status dots kept (GONE) so the adapter's findViewById stays valid -->
        <ImageView
            android:id="@+id/f_status_icon"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:visibility="gone" />

        <ImageView
            android:id="@+id/f_relay_icon"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:visibility="gone" />

        <LinearLayout
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:layout_centerVertical="true"
            android:layout_toRightOf="@id/f_avatar_icon"
            android:gravity="center_vertical"
            android:orientation="vertical">

            <TextView
                android:id="@+id/textViewName"
                android:layout_width="fill_parent"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:maxLines="1"
                android:text="example name"
                android:textColor="@color/tg_chat_title"
                android:textSize="16sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/textViewStatus"
                android:layout_width="fill_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:ellipsize="end"
                android:maxLines="1"
                android:text="@string/group_info_member_offline"
                android:textColor="@color/tg_chat_preview"
                android:textSize="14sp" />

        </LinearLayout>

    </RelativeLayout>

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_marginLeft="80dp"
        android:background="@color/md_grey_300_seperator" />

</LinearLayout>
