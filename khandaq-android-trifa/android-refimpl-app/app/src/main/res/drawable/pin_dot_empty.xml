<?xml version="1.0" encoding="utf-8"?>
<!-- KHANDAQ PIN indicator (empty): hollow teal ring. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@android:color/transparent" />
    <stroke
        android:width="1.5dp"
        android:color="@color/khandaq_teal" />
    <size
        android:width="14dp"
        android:height="14dp" />
</shape>
