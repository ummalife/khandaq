# KHANDAQ — the push relay's DIRECT dependencies. requirements.txt is the hash-locked closure
# derived from this file; regenerate it with scripts/lock-relay-requirements.py, never by hand.
flask==3.1.3
requests==2.34.2
gunicorn==26.1.0
google-auth==2.56.3
